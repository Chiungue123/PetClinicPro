# Server Settings
server:
  port: 8081

# Database Settings
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_clinic_db
    username: root
    password: root1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect